import React from 'react';
import VisitorHeader from '../../Components/VistorHeader';
import Home from '../../Pages/Visitor/Home';
import { BrowserRouter, Router, Routes, Route } from 'react-router-dom';
import Login from '../../Pages/Visitor/Login/Login';
import SignUp from '../../Pages/Visitor/Register/SignUp';
import { themeLogin, themeSignup, theme } from './../../Themes/index';

import { useTheme, Stack, ThemeProvider, Box, IconButton, createTheme, Button } from '@mui/material';
function IndexVistor ()
{
    return (

        null

    );
}

export default IndexVistor;